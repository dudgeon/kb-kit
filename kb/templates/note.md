---
type: note
summary: Default type — one distilled piece of knowledge, one file.
statuses: [draft, solid]
---

# note

The default. Use when nothing more specific fits. One topic per file; if a
note grows two topics, split it and keep the links. A `draft` note is a
placeholder someone should improve; `solid` means a human stands behind it.
