---
type: source
summary: An external artifact (article, thread, spec, paper) ingested into this KB.
statuses: [unread, read, processed]
---

# source

One page per ingested artifact. Frontmatter should carry `url` and `date`
(of the artifact, not the ingest). Body: a distilled summary first, then a
`## Raw` or `## Key excerpts` section with verbatim material — never edit
inside that section; corrections go above it, attributed. `processed` means
its ideas have been propagated to the relevant topic/note pages.
