---
type: standard
summary: A published spec or convention (demo type — remove via setup if unneeded).
---

# standard

For specs, formats, and conventions. Frontmatter should carry `steward` (who
governs it), `status_note` (draft/stable/adopted), and `url`. Body: what it
specifies, adoption reality vs claims, and how it relates to neighboring
standards (links).
