---
type: tool
summary: A product or system in the landscape (demo type — remove via setup if unneeded).
---

# tool

For products, platforms, and systems. Frontmatter should carry `maker` and
`url`. Body: what it does, the idea it embodies (link the topic page), and
its relationship to the standards it implements or ignores.
